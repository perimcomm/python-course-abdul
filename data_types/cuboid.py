length = float(input("Type length: "))
heigth = float(input("Type heigth: "))
breadth = float(input("Type breadth: "))

cuboid_total_area = 2 * ( length * breadth + length * heigth + breadth * heigth)

print(cuboid_total_area)